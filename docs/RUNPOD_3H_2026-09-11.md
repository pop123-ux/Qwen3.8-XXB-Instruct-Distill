# RunPod L40S — 3-hour RQ1 session (2026-09-11)

## Purpose

Use one paid 48 GB L40S window to maximize **completed, interpretable RQ1 evidence**.
This branch starts from the latest results lineage (`prep/l40s-rq1-scale1024` at
`4eb843f937be519e3df958fe0bc847d93777d031`), which already contains Run003 through
Run007 evidence.

Do not merge or silently mix the divergent hardened `claude/qwen38-16gb-distill-t6d0gy`
protocol lineage during the paid window. Branch reconciliation is bookkeeping and must not
consume scarce GPU time or alter an active experiment.

## User-selected runtime policy

For this session, Python ML packages are deliberately installed **without version pins**
(`pip install torch transformers ...`). This is a convenience/runtime choice, not a claim
that the resulting environment is identical to historical runs.

Consequences:

1. Capture Python, PyTorch, Transformers, CUDA, driver, GPU, `pip freeze`, and git SHA before
   training.
2. All new matched comparisons in this session must use the same environment.
3. Do not make throughput claims against historical runs if the software stack differs.
4. Prefer same-session paired comparisons for quality claims where practical.
5. Never compensate for an environment incompatibility by changing the scientific protocol.

`scripts/runpod_bootstrap_3h.sh` implements this policy.

## Frozen scientific assets

- Teacher: `Qwen/Qwen3.8-27B`
- Teacher revision: `dbdc473dea0d6a9763042881cc33d6058d1742d2`
- Teacher local path: `/workspace/models/qwen3.8-27b-dbdc473`
- Canonical student: `/workspace/runs/pilot001/transferred`
- Corpus: `/workspace/corpora/gutenberg/train.txt`
- Corpus SHA-256: `bc5972d9a52580ff14ab1b3b1753f9cd68c726c63cc625a7ed3913ec3c5dc5c5`
- Corpus bytes: `5,554,406`
- Packed 700k-token identity from prior runs: `e11ca38bb099fc89c2f74e96f5d2f1209def6a16f6a8432d4e9972acd50c100d`
- Sequence length: 1536
- Max corpus tokens: 700000
- Batch: 1
- Gradient accumulation: 1
- LR: `2e-4`
- Optimizer: AdamW
- Precision: bf16
- QLoRA: rank 16, alpha 32, historical dropout 0.05
- KD temperature: 2.0
- KD top-k: 64
- Layer-pair chunking: 4
- VRAM hard ceiling: 45 GiB

Historical Run003–Run007 artifacts are immutable.

## First required experiments

### Run008 — raw residual delta, seed 1

Exact seed-only replication of Run006. Run with:

```bash
python scripts/guard_vram.py --max-vram-gib 45 --interval 5 \
  --log /workspace/runpod_sessions/2026-09-11-rq1-3h/run008_raw_delta_seed1_vram.log \
  -- python scripts/run008_raw_delta_seed1.py
```

### Run009 — pointwise KD, seed 1

Same-session matched pointwise control for Run008. Run with:

```bash
python scripts/guard_vram.py --max-vram-gib 45 --interval 5 \
  --log /workspace/runpod_sessions/2026-09-11-rq1-3h/run009_pointwise_seed1_vram.log \
  -- python scripts/run009_pointwise_seed1.py
```

Or run both sequentially:

```bash
bash scripts/runpod_run_seed1_pair.sh
```

The guard uses `nvidia-smi`; it does not allocate CUDA memory. The training summaries remain
the authoritative framework-level memory record.

## Adaptive queue after the seed-1 pair

Do **not** spend the paid window doing extensive post-hoc analysis while a complete useful
experiment can run. Preserve artifacts and make only the minimum classification needed to
select the next admissible branch.

1. If raw-delta seed 1 beats or is credibly non-inferior to pointwise seed 1, mark the
   replication result and advance to the next roadmap experiment that can complete inside
   the remaining window. A second heterogeneous teacher/student topology has higher
   scientific value than inventing another loss.
2. If raw-delta seed 1 is ambiguous, preserve it and use at most one bounded clarification.
   Do not tune the objective.
3. If raw-delta seed 1 clearly loses, verify integrity and the same-session control. If the
   run is clean, retain the negative seed result; do not rescue it by hyperparameter search.
4. A 1024-step pointwise control is scientifically useful but historically takes roughly
   two hours. Start it only when the remaining wall-clock budget is sufficient to finish,
   archive, and validate it. Prefer complete 128-step experiments otherwise.
5. If a second topology is not executable without substantial engineering, a complete
   treatment/control pair at another seed is preferable to an unfinished topology run.

## GPU-time rules

- Hard paid window: 3 hours.
- Keep the GPU occupied with complete RQ-relevant experiments.
- CPU-side state updates, checksums, summaries, and git bookkeeping may run while the next
  GPU experiment is executing, but must not interfere with CUDA.
- Check `nvidia-smi` before each run. The repository VRAM guard monitors during the run.
- Do not launch separate Python CUDA probes while training.
- Do not reduce sequence length, batch size, objective terms, LoRA geometry, or teacher
  fidelity to make a run fit. A VRAM breach is a recorded technical result, not permission
  to change the experiment.
- Do not overwrite completed run directories.

## Short Claude handoff prompt

After resuming the latest Claude Code session for this project, use:

> We are now on a RunPod L40S with a hard 3-hour paid GPU window. Continue the existing RQ1 research roadmap and maximize completed, interpretable evidence per minute. Start by verifying this branch/session setup, then execute Run008 raw-delta seed 1 exactly as preregistered and immediately pair it with the matched pointwise seed-1 control. Preserve every completed/failed run and update the research state as you go. After that, select the next admissible high-information experiment that can fully finish inside the remaining time; do not invent or tune objectives just to obtain a positive result. All runs today must stay in this same unpinned-but-recorded Python environment and keep the frozen teacher/student/data/training invariants. Use `nvidia-smi` before each run and the existing 45 GiB `nvidia-smi`-based VRAM guard during training; do not run CUDA memory probes that perturb training. Keep the GPU busy and do bookkeeping on CPU in parallel when safe.

## Fresh-box command sequence

See the ChatGPT handoff that prepared this branch for the exact system-package, GitHub,
Claude Code, Python dependency, bootstrap, and launch commands. The repository-local step is:

```bash
cd /workspace/Qwen3.8-XXB-Instruct-Distill
git switch runpod/rq1-3h-2026-09-11
bash scripts/runpod_bootstrap_3h.sh
claude
```
