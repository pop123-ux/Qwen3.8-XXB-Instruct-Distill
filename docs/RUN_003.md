# Run 003 — Layer / Intermediate KD control

**Status: the memory gate is now PASSED, at 38.0289 GiB. The 128-step run is ready to
launch and had not been launched when this was written.**

> The first calibration failed the gate at 42.5354 GiB and the account of it below is
> kept unchanged. What followed is in [the second calibration](#the-second-calibration-chunked-evaluation)
> at the end of this document.

Run 003 is the matched partner of Run 002. Everything is held equal and the single
intended difference is the distillation objective:

| | Run 002 | Run 003 |
| --- | --- | --- |
| objective | pure logit KD | pure layer / intermediate KD |
| supervision | KL(teacher ‖ student) over the output distribution | pointwise hidden-state matching at 48 mapped layer pairs |
| everything else | — | identical |

The objective is defined in full in [LAYER_KD.md](LAYER_KD.md) and copied into the run
record under `distillation.layer_kd_definition`.

## The calibration

One optimizer step at sequence length 1536, run to measure peak VRAM before committing to
128 steps at an objective never previously exercised.

Launch SHA `301e183c7739be99b9c7d32ca3d4a6eb6f7ec303`.
Record: `experiments/run003_calibration_1536/`.

| | Run 002 (logit KD) | Run 003 (layer KD) | difference |
| --- | ---: | ---: | ---: |
| peak allocated | 40.4010 GiB | **42.5354 GiB** | +2.1344 |
| peak reserved | 40.9453 GiB | **44.0996 GiB** | +3.1543 |
| A40 usable | 44.4316 GiB | 44.4316 GiB | — |
| safety gate | 42.0 GiB | 42.0 GiB | — |

```
GATE FAILED: peak allocated 42.5354 GiB > 42.0 GiB      (margin -0.5354 GiB)
peak reserved 44.0996 GiB against 44.4316 GiB usable    (headroom  0.3320 GiB)
```

The run therefore **stopped**, exactly as Run 002's first attempt at 2048 tokens did. The
sequence length was **not** reduced: 1536 is Run 002's sequence length, and changing it
would break the one property that makes the two arms comparable.

## Where the memory went

Both objectives run the same teacher, the same quantised student, the same optimizer and
the same batch. The stage profile locates the whole difference in the loss:

| stage | Run 002 | Run 003 |
| --- | ---: | ---: |
| baseline (resident 4-bit teacher) | 16.4414 | 16.4414 |
| after model creation | 32.5967 | 32.5967 |
| after forward | 36.1323 | 37.3225 |
| **after loss** | **38.3154** | **42.5354** |
| after backward | 40.4010 | 42.5354 |

Two costs, both intrinsic to the objective rather than to this implementation:

1. **The teacher must return its hidden states.** 65 tensors of (1, 1536, 5120); about a
   gigabyte, and the reason `after_forward` is 1.19 GiB higher. It is dropped as soon as
   the loss is built, so it does not persist into the backward pass.
2. **Both sides' hidden states are held through the backward pass.** The loss compares 48
   pairs, and `mse_loss` saves both of its normalised float32 inputs for backward: 48 × 2 ×
   (1536 × 5120 × 4 bytes) ≈ 3.0 GiB that cannot be freed until the gradient is computed.
   This is why `after_loss` is 4.22 GiB higher and why the peak sits at the loss rather
   than at the backward pass, which is where logit KD peaks.

Gradient checkpointing does not help here: the retained tensors are loss inputs, not layer
activations.

## What the calibration nevertheless established

The step ran to completion, so the objective and its plumbing are verified even though the
128-step run did not launch:

- The layer map built at runtime is the documented one — **48 pairs, 16 teacher layers
  unsupervised** (8–11, 24–27, 36–39, 52–55).
- The optimised loss is the layer term: `loss 1.4501 = magnitude 0.9668 + direction
  0.4834`, and not the KD divergence reported beside it (7.190).
- `layer_norm_ratio` at step 1 is **0.5238** — the student's mapped hidden states carry
  roughly half the teacher's activation norm at initialisation. A magnitude-dominated
  objective at the start, which is what the separate magnitude/direction reporting exists
  to show.
- **The arms are genuinely matched.** Run 003's step-1 KD diagnostic is `7.190154` and Run
  002's step-1 KD loss is `7.190154`, to six decimal places. Same seed, same batch, same
  teacher, same student initialisation — the two runs saw an identical first step and
  differ only in what was optimised from it.

## Options, none of them taken

Every way forward changes something the comparison depends on, so none was chosen here.
The decision belongs to the project owner.

1. **Raise the gate to ~43 GiB.** The measurement fits the card (44.10 GiB reserved against
   44.43 usable) with 0.33 GiB of headroom. The gate is a safety margin, not a hardware
   limit, and it was set for a different objective. But 0.33 GiB is thin: allocator
   fragmentation over 128 steps is exactly the risk the margin exists for, and an OOM at
   step 90 would waste more budget than a rerun.
2. **Reduce the sequence length.** Explicitly forbidden — it would break the match with Run
   002 and is the reason the instruction says not to do it silently.
3. **Reduce the number of supervised pairs** (for example, one pair per hybrid group rather
   than all 48). This would cut the retained loss inputs roughly four-fold and comfortably
   clear the gate, but it changes the objective, so Run 003 would no longer be *the*
   conventional layer-matching control.
4. **Compute the layer term in chunks over positions**, accumulating gradient per chunk.
   This is numerically the same objective and would cut peak memory substantially, but it
   is new training-loop machinery that would have to be written and validated first, and
   Run 003 would then be measuring code written for it rather than the existing A1 control.

Option 1 is the only one that leaves the science untouched. Option 4 is the only one that
buys real headroom without touching the science, at the cost of new code.

## Not done

- The 128-step Run 003 was not launched.
- No retry was attempted.
- Run 004 (computational-behaviour / state KD) was not started.


## The second calibration — chunked evaluation

Option 4 above was taken, with one change of axis: it proposed chunking **over
positions**, and what was implemented chunks **over pairs** — the gradient is taken every 4
mapped pairs rather than after all 48. Pairs are the better axis here. Chunking positions
would split each pair's MSE and cosine means into partial sums and reassociate them, which
is exact in algebra but not in float32; chunking pairs leaves every per-pair reduction
running over the whole 1536-token sequence exactly as before, and only the outer mean over
pairs is rescheduled. That is why the per-pair terms come out bit-identical rather than
merely close.

**The objective did not change.** The same 48 mapped pairs over the same full 1536-token
sequence, the same per-token RMS normalisation, the same per-pair magnitude and direction
terms, the same `direction_weight`, the same mean over pairs. The gate was not raised, the
sequence length is still 1536, and no pair was dropped. What moved is when the gradient is
taken. Full account in [LAYER_KD_CHUNKING.md](LAYER_KD_CHUNKING.md).

Equivalence was established *before* this calibration, on the first calibration's own
batch with the real teacher and student:

- **the gradient with respect to the student's hidden states is bit-identical** at all 48
  supervised layers — 0.0 absolute, 0.0 relative;
- the objective's value differs by 6.424e-08 relative, which is float32 summation order
  and carries no gradient;
- the per-layer diagnostics are identical, 48 of 48;
- end to end through the student, over four passes per form, the true form-based split
  ranks 10 of 35 by mean separation (p = 0.29) — indistinguishable from the student's own
  backward, which is not bit-reproducible and differs from itself by up to 9.77e-03.

Records: `experiments/run003_chunking_equivalence/`.
Launch SHA `2ea4cdb222ffa24eb5c38b5592a42fdf8b8eadfb`.
Record: `experiments/run003_calibration_1536_chunked/`.

| | unchunked | chunked (4 pairs) | difference |
| --- | ---: | ---: | ---: |
| peak allocated | 42.5354 GiB | **38.0289 GiB** | -4.5065 |
| peak reserved | 44.0996 GiB | **39.8906 GiB** | -4.2090 |
| A40 usable | 44.4316 GiB | 44.4316 GiB | — |
| safety gate | 42.0 GiB | 42.0 GiB | — |

```
GATE PASSED: peak allocated 38.0289 GiB <= 42.0 GiB   (margin 3.9711 GiB)
peak reserved 39.8906 GiB against 44.4316 GiB usable   (headroom 4.5410 GiB)
```

Stage profile:

| stage | allocated GiB |
| --- | ---: |
| baseline | 16.4414 |
| after_model_creation | 32.5591 |
| after_model_to_device | 32.5591 |
| after_optimizer_creation | 32.5591 |
| after_input_allocation | 32.5591 |
| after_backward | 34.1043 |
| after_optimizer_step | 34.2757 |
| peak_training | 34.2757 |
| after_loss | 34.7289 |
| after_forward | 35.0207 |

The step reproduced the first calibration exactly where it should: `loss 1.4501 =
magnitude 0.9668 + direction 0.4834`, KD diagnostic `7.190`, CE `10.960`, 48 pairs with 16
teacher layers unsupervised. Throughput 129 tok/s against the unchunked 133, so the chunked
form costs roughly 3% of step time for 4.51 GiB.

## Still not done

- **The 128-step Run 003 was not launched.** The gate is passed and the run is ready, but
  the machine was being shut down and a ~30-minute run would have been killed mid-flight.
- Run 004 (computational-behaviour / state KD) was not started.
